---
title: 'Enviar Botões Interativos'
openapi: 'POST /messages/interactive/send-buttons'
---

## Request

```bash
curl -X POST https://api.chatvolt.ai/messages/interactive/send-buttons \
  -H "Authorization: Bearer $CHATVOLT_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "to": "5511988887777",
    "bodyText": "Escolha uma opção:",
    "buttons": [
      { "id": "btn_sim", "title": "Sim" },
      { "id": "btn_nao", "title": "Não" }
    ]
  }'
```

### Parâmetros

| Campo | Tipo | Obrigatório | Descrição |
|-------|------|:-----------:|-----------|
| `to` | string |  | Telefone do destinatário |
| `bodyText` | string |  | Texto da mensagem |
| `buttons` | array |  | Lista de botões (máx: 3) |
| `footerText` | string |  | Texto do rodapé |

## Dicas

- **Máximo de 3 botões** por mensagem, com até 20 caracteres de título.
- Os `id`s dos botões são recebidos de volta quando o usuário toca — use para rotear a resposta no seu fluxo.
- Botões interativos só funcionam em conversas iniciadas (não são válidos como primeira mensagem — use template).

## Resposta

```json
{
  "data": {
    "messageId": "wamid_abc123",
    "status": "sent"
  },
  "success": true,
  "error": null
}
```
