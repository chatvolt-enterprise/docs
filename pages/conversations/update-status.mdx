---
title: 'Atualizar Status'
openapi: 'POST /conversations/{conversationId}/set-status'
---

Atualiza o status de uma conversa (aberta, resolvida, etc.).

## Request

```bash
curl -X POST https://api.chatvolt.ai/conversations/123/set-status \
  -H "Authorization: Bearer $CHATVOLT_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "status": "RESOLVED"
  }'
```

### Parâmetros de Path

| Parâmetro | Descrição |
|-----------|-----------|
| `conversationId` | ID da conversa |

### Parâmetros do Body

| Campo | Tipo | Obrigatório | Descrição |
|-------|------|:-----------:|-----------|
| `status` | string | ✓ | Novo status: `RESOLVED`, `UNRESOLVED` ou `HUMAN_REQUESTED` |

## Response

```json
{
  "success": true
}
```

## Dicas

- **Os valores são maiúsculos**: `RESOLVED`, `UNRESOLVED` ou `HUMAN_REQUESTED` — minúsculas falham na validação.
- **`RESOLVED` encerra a conversa** — ela sai da fila de atendimento ativa; consulte o formato completo via `GET /conversation/{conversationId}`.
- Use `UNRESOLVED` para reabrir e `HUMAN_REQUESTED` para pedir intervenção humana.
