---
title: 'Criar Contato'
openapi: 'POST /contacts'
---

## Request

```bash
curl -X POST https://api.chatvolt.ai/contacts \
  -H "Authorization: Bearer $CHATVOLT_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "joao@exemplo.com",
    "name": "João Silva",
    "phone": "5511988887777"
  }'
```

### Parâmetros

| Campo | Tipo | Obrigatório | Descrição |
|-------|------|:-----------:|-----------|
| `email` | string |  | Email do contato |
| `name` | string |  | Nome completo |
| `phone` | string |  | Telefone (com DDI) |
| `organizationName` | string |  | Empresa |
| `notes` | string |  | Observações |

## Resposta

```json
{
  "data": {
    "id": "contact_abc123",
    "email": "joao@exemplo.com",
    "name": "João Silva",
    "phone": "5511988887777",
    "createdAt": "2026-07-23T10:00:00Z"
  },
  "success": true,
  "error": null
}
```

## Dicas

- **`phone` com DDI sem `+`** (ex.: `5511988887777`) — é o formato usado para casar com conversas do WhatsApp.
- O `contactId` retornado pode ser passado em `POST /agents/{id}/query` para identificar o cliente no atendimento.
- Contatos são deduplicados por e-mail/telefone: se já existir, a resposta retorna o contato existente com os dados atualizados.
